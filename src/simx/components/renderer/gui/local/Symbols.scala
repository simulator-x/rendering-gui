package simx.components.renderer.gui.local

import simx.core.ontology.types.OntologySymbol

object Symbols {

  object playerHit extends OntologySymbol(Symbol("playerHit"))

  object buttonPressed extends OntologySymbol(Symbol("ButtonPressed"))

  object isClickable extends OntologySymbol(Symbol("IsClickable"))
  object isDraggable extends OntologySymbol(Symbol("IsDraggable"))

  object explosion extends OntologySymbol(Symbol("Explosion"))

  object notification extends OntologySymbol(Symbol("Notification"))

  object overlayText extends OntologySymbol(Symbol("overlayText"))
  object overlayTexture extends OntologySymbol(Symbol("overlayTexture"))
}

